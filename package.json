{
  "name": "maxcanva",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n server,client -c magenta,cyan \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run typecheck --workspace shared && npm run typecheck --workspace server && npm run build --workspace client",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
